/** All cache TTL values in seconds — no magic numbers */
export enum CacheTtl {
  ONE_MINUTE      = 60,
  FIVE_MINUTES    = 300,
  FIFTEEN_MINUTES = 900,
  ONE_HOUR        = 3600,
  ONE_DAY         = 86400,
}
